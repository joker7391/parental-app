import React from "react";
import { View, Text, TouchableOpacity } from "react-native";

const index = () => {
  return (
    <View>
      <TouchableOpacity>
        <Text className="text-red-400">Profile page</Text>
      </TouchableOpacity>
    </View>
  );
};

export default index;
